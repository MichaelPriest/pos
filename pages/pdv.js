import Head from 'next/head';
import Link from 'next/link';
import { useEffect, useMemo, useState } from 'react';
import AuthGuard from '../components/AuthGuard';
import { db } from '../lib/supabase';
const money=n=>Number(n||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});
export default function Pdv(){const[products,setProducts]=useState([]),[cart,setCart]=useState([]),[query,setQuery]=useState(''),[category,setCategory]=useState('Todos'),[discount,setDiscount]=useState(0),[payment,setPayment]=useState('pix'),[customer,setCustomer]=useState(''),[note,setNote]=useState(''),[notice,setNotice]=useState(''),[finishing,setFinishing]=useState(false);useEffect(()=>{db.products().then(setProducts).catch(e=>setNotice(e.message))},[]);const filtered=useMemo(()=>products.filter(p=>(category==='Todos'||p.category===category)&&p.name.toLowerCase().includes(query.toLowerCase())),[products,category,query]);const subtotal=cart.reduce((s,p)=>s+Number(p.price),0),total=Math.max(0,subtotal-discount);const add=p=>{if(cart.filter(x=>x.id===p.id).length>=p.stock)return setNotice('Quantidade máxima em estoque atingida.');setCart([...cart,p])};const finish=async()=>{if(!cart.length)return;setFinishing(true);try{const id=await db.posCheckout({items:cart.map(p=>({product_id:p.id,quantity:1})),payment_method:payment,customer_name:customer||'Consumidor final',discount:Number(discount),notes:note});setNotice(`Venda #${String(id).slice(0,8)} concluída com sucesso.`);setCart([]);setDiscount(0);setCustomer('');db.products().then(setProducts)}catch(e){setNotice(e.message)}finally{setFinishing(false)}};return <AuthGuard roles={['admin','manager','cashier']}><Head><title>PDV | ReVeste</title></Head><main className="pos-page"><header><Link href="/admin">← Painel</Link><div className="brand"><span className="brand-mark">R</span><span>ReVeste <small>PDV</small></span></div><span className="pos-online">● Caixa online</span></header><section><div className="pos-catalog"><div className="pos-heading"><div><p className="eyebrow">VENDA PRESENCIAL</p><h1>Adicionar produtos</h1></div><div className="pos-search">⌕ <input autoFocus value={query} onChange={e=>setQuery(e.target.value)} placeholder="Buscar por nome ou código..."/></div></div><div className="category-pills">{['Todos',...new Set(products.map(p=>p.category))].map(c=><button className={c===category?'active':''} onClick={()=>setCategory(c)} key={c}>{c}</button>)}</div>{notice&&<div className="admin-notice">{notice}<button onClick={()=>setNotice('')}>×</button></div>}<div className="pos-products">{filtered.map(p=><button key={p.id} onClick={()=>add(p)}><img src={p.image_url||'/placeholder.svg'} alt=""/><span><small>{p.category} · {p.size}</small><strong>{p.name}</strong><b>{money(p.price)}</b><em>{p.stock} disp.</em></span><i>＋</i></button>)}</div></div><aside className="pos-cart"><div><h2>Venda atual</h2><span>{cart.length} itens</span></div><label>Cliente (opcional)<input value={customer} onChange={e=>setCustomer(e.target.value)} placeholder="Nome do cliente"/></label><div className="pos-items">{cart.length?cart.map((p,i)=><article key={`${p.id}-${i}`}><img src={p.image_url||'/placeholder.svg'} alt=""/><span><strong>{p.name}</strong><small>Tam. {p.size}</small><b>{money(p.price)}</b></span><button onClick={()=>setCart(cart.filter((_,x)=>x!==i))}>×</button></article>):<div className="empty-pos">▢<p>Nenhum produto adicionado</p><small>Selecione uma peça ao lado para começar.</small></div>}</div><div className="pos-totals"><p><span>Subtotal</span><b>{money(subtotal)}</b></p><label><span>Desconto</span><input type="number" min="0" max={subtotal} value={discount} onChange={e=>setDiscount(Number(e.target.value))}/></label><p className="total"><span>Total</span><b>{money(total)}</b></p></div><div className="pos-payments"><span>Forma de pagamento</span>{['pix','cartão','dinheiro'].map(x=><button className={payment===x?'active':''} onClick={()=>setPayment(x)} key={x}>{x==='pix'?'◇':x==='cartão'?'▭':'$'}<small>{x}</small></button>)}</div><label>Observação<input value={note} onChange={e=>setNote(e.target.value)} placeholder="Opcional"/></label><button disabled={!cart.length||finishing} className="pos-finish" onClick={finish}>{finishing?'Finalizando...':'Concluir venda'} <span>{money(total)}</span></button></aside></section></main></AuthGuard>}
