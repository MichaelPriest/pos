import{useEffect,useMemo,useState}from'react';import AuthGuard from'../components/AuthGuard';import{db}from'../lib/supabase';
export default function HR(){const[employees,setEmployees]=useState([]),[entries,setEntries]=useState([]);useEffect(()=>{Promise.all([db.employees(),db.timeEntries()]).then(([e,t])=>{setEmployees(e);setEntries(t)})},[]);const hours=useMemo(()=>entries.filter(e=>e.clock_out).reduce((s,e)=>s+(new Date(e.clock_out)-new Date(e.clock_in))/36e5,0),[entries]);return <AuthGuard roles={['admin','manager']}><main className="ops-page"><header className="ops-head"><div><p className="eyebrow">RECURSOS HUMANOS</p><h1>RH e jornada</h1><span>Visão de quadro, departamentos, admissões e horas registradas.</span></div></header><section className="ops-kpis"><article><small>Funcionários ativos</small><strong>{employees.filter(e=>e.employee_details?.[0]?.employment_status!=='terminated').length}</strong></article><article><small>Departamentos</small><strong>{new Set(employees.map(e=>e.employee_details?.[0]?.department).filter(Boolean)).size}</strong></article><article><small>Horas registradas</small><strong>{hours.toFixed(1)}h</strong></article><article><small>Em jornada agora</small><strong>{entries.filter(e=>!e.clock_out).length}</strong></article></section><section className="ops-card ops-table"><h2>Quadro de pessoal</h2><div className="table-scroll"><table><thead><tr><th>Funcionário</th><th>Cargo</th><th>Departamento</th><th>Admissão</th><th>Situação</th></tr></thead><tbody>{employees.map(e=>{const detail=e.employee_details?.[0]||{};return <tr key={e.id}><td><b>{e.name}</b><small>{e.email}</small></td><td>{detail.job_title||e.role}</td><td>{detail.department||'Loja'}</td><td>{detail.admission_date?new Date(`${detail.admission_date}T12:00`).toLocaleDateString('pt-BR'):'—'}</td><td>{detail.employment_status==='leave'?'Afastado':detail.employment_status==='terminated'?'Desligado':'Ativo'}</td></tr>})}</tbody></table></div></section></main></AuthGuard>}
