import{useEffect,useMemo,useState}from'react';import AuthGuard from'../components/AuthGuard';import{db}from'../lib/supabase';
export default function Audit(){const[logs,setLogs]=useState([]),[query,setQuery]=useState(''),[entity,setEntity]=useState('todos'),[action,setAction]=useState('todos');useEffect(()=>{db.auditLogs().then(setLogs)},[]);const entities=[...new Set(logs.map(x=>x.entity_type))],filtered=useMemo(()=>logs.filter(x=>(entity==='todos'||x.entity_type===entity)&&(action==='todos'||x.action===action)&&JSON.stringify(x).toLowerCase().includes(query.toLowerCase())),[logs,query,entity,action]);return <AuthGuard roles={['admin']}><main className="ops-page"><header className="ops-head"><div><p className="eyebrow">GOVERNANÇA E SEGURANÇA</p><h1>Auditoria</h1><span>Histórico imutável das operações críticas do sistema.</span></div></header><section className="report-filters"><label>Pesquisar<input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Usuário, registro ou conteúdo"/></label><label>Entidade<select value={entity} onChange={e=>setEntity(e.target.value)}><option value="todos">Todas</option>{entities.map(x=><option key={x}>{x}</option>)}</select></label><label>Ação<select value={action} onChange={e=>setAction(e.target.value)}><option value="todos">Todas</option><option>INSERT</option><option>UPDATE</option><option>DELETE</option></select></label></section><section className="ops-card ops-table"><h2>{filtered.length} eventos</h2><div className="table-scroll"><table><thead><tr><th>Data</th><th>Ação</th><th>Entidade</th><th>Registro</th><th>Responsável</th></tr></thead><tbody>{filtered.map(log=><tr key={log.id}><td>{new Date(log.created_at).toLocaleString('pt-BR')}</td><td><b>{log.action}</b></td><td>{log.entity_type}</td><td>{log.entity_id?.slice(0,12)}</td><td>{log.actor_id?.slice(0,8)||'Sistema'}</td></tr>)}</tbody></table></div></section></main></AuthGuard>}
