import Head from'../../src/shims/Head';import{useRouter}from'../../src/shims/router';import{useEffect,useState}from'react';import AuthGuard from'../../components/AuthGuard';import{db}from'../../lib/supabase';const money=n=>Number(n||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});export default function Receipt(){const{id}=useRouter().query,[order,setOrder]=useState(null);useEffect(()=>{if(id)db.order(id).then(setOrder)},[id]);return <AuthGuard roles={['admin','manager','cashier']}><Head><title>Comprovante de venda</title></Head><main className="receipt-page">{order&&<><header><b>ReVeste</b><span>{order.receipt_type==='fiscal'?'CUPOM FISCAL — AGUARDANDO AUTORIZAÇÃO':'COMPROVANTE NÃO FISCAL'}</span></header><p>Venda #{order.id.slice(0,8)}</p><p>{new Date(order.created_at).toLocaleString('pt-BR')}</p><p>Cliente: {order.customer_name}</p>{order.customer_document&&<p>CPF/CNPJ: {order.customer_document}</p>}<hr/>{order.order_items.map(i=><div className="receipt-item" key={i.id}><span>{i.quantity}x {i.products?.name}<small>Tam. {i.products?.size}</small></span><b>{money(i.unit_price*i.quantity)}</b></div>)}<hr/><div className="receipt-total"><span>TOTAL</span><b>{money(order.total)}</b></div><p>Pagamento: {order.payment_method}</p>{order.receipt_type==='fiscal'&&<div className="fiscal-warning">Este documento somente possui validade fiscal após autorização pelo provedor fiscal/SEFAZ. Não substitui NFC-e ou SAT.</div>}<footer>Obrigado pela preferência!<br/>Moda circular, histórias que continuam.</footer><button className="no-print" onClick={()=>print()}>🖨️ Imprimir comprovante</button></>}</main></AuthGuard>}
